# Server basics
APP_ENV=local
LOG_LEVEL=info

# Database (set when DB is added)
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@localhost:5432/ai_ta

# AWS / Cognito (optional for MVP auth)
AWS_REGION=us-east-1
COGNITO_ISSUER_URL=
COGNITO_AUDIENCE=

# S3 (for resources)
S3_BUCKET_NAME=ai-teaching-assistant-dev

# Bedrock Knowledge Base (if using managed RAG)
BEDROCK_KB_ID=


